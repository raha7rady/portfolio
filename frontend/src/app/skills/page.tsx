import type { Metadata } from "next";
import SkillsContent from "@/components/sections/SkillsContent";

export const metadata: Metadata = {
  title: "مهارت‌ها | ساناز",
};

export default function SkillsPage() {
  return <SkillsContent />;
}
